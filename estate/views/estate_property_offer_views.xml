<odoo>
    <!--List view for estate property offer model-->
    <record id="estate_property_tag_offer_view" model="ir.ui.view">
        <field name="name">estate.property.offer.list.view</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Property Offers" editable="bottom" decoration-success="status == 'accepted'"
            decoration-danger="status == 'refused'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="accept_offer_button" type="object" icon="fa-check" invisible="status in ('accepted', 'refused')"/>
                <button name="refuse_offer_button" type="object" icon="fa-times" invisible="status in ('accepted', 'refused')"/>
                <field name="status"/>
            </list>
        </field>
    </record>

    <!--Form view for estate property offer model-->
    <record id="estate_property_offer_form_view" model="ir.ui.view">
        <field name="name">estate.property.offer.form.view</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="price"/>
                    <field name="partner_id"/>
                    <field name="validity"/>
                    <field name="date_deadline"/>
                    <field name="status"/>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
